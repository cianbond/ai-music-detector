# Audio data (large, regenerable from metadata via the download script)
data/audio/
*.mp3
*.wav

# Large source CSVs (downloadable from the SONICS dataset)
data/fake_songs.csv
data/real_songs.csv

# (subset_splits.csv is intentionally not ignored.  its small and its the artifact

# Python / Jupyter
__pycache__/
*.pyc
.ipynb_checkpoints/

# Environments
.venv/
env/
venv/

# Editor / OS
.vscode/
.DS_Store
Thumbs.db

# SONICS' own premade split files (not used. Have built own split)
data/train.csv
data/test.csv
data/valid.csv

# Generated preprocessing outputs
data/spectrograms/
data/spectrograms.zip
data/spectrograms_10s/
spectrograms_10s.zip
data/fakemusiccaps/
__MACOSX/
   
# (failed_downloads.csv is intentionally tracked and the 277 reals that failed to download are documented)


